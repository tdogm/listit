export async function postToEtsy({ listing, photos }) {
  // TODO: implement once you set env vars
  return {
    ok: false,
    manual: true,
    message: "Etsy connector not configured yet (missing env vars).",
  };
}
